# Modelos

`contracts.js`: campos, enums e validação de comandos.
`ledger.js`: sinal das linhas contábeis e segurança dos saldos por data.
Contratos completos e política de migration em `docs/domain.md`.
